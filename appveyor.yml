version: 0.1.{build}-dev

shallow_clone: true

environment:
  LUAROCKS_VER: 2.4.1
  LUA_VER: 5.1.4
  CURRENT_BRANCH: develop

platform:
  - x86
  - x64

cache:
  - c:\lua -> appveyor.yml
  - c:\external -> appveyor.yml

# Skip unsupported combinations (ie, VS2008 for x64 is not supported)
init:
- if "%platform%" EQU "x64" ( for %%a in (2008 2010 MinGW) do ( if "%Configuration%"=="%%a" (echo "Skipping unsupported configuration" && exit /b 1 ) ) )

install:
# Download appveyor templates from lidesdk repos
- git clone https://github.com/lidesdk/appveyor_lua.git .appveyor
# Make compiler command line tools available
- call .appveyor\set_compiler_env.bat
# Setup Lua development/build environment
- call .appveyor\install.bat

#before_build:
## @todo
#- echo "Installing external deps"

build_script:
  - echo "Building..."
  #- luarocks install luafilesystem
  #- git clone https://github.com/lidesdk/framework.git lide_framework && cd lide_framework && git checkout %CURRENT_BRANCH%

test_script:
- echo "Testing..."
- set LIDE_PATH=%CD%
- lua tests/lide_core_oop_tests.lua
- lua tests/lide_base_file_tests.lua
- lua tests/lide_core_error_tests.lua